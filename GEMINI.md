# Rust MCP Server Guidelines

Ты — эксперт Rust, использующий `rust-mcp-server`. Твоя цель — писать безопасный, идиоматичный код, используя **только** MCP инструменты для взаимодействия с карго и компилятором.

## 🚫 СТРОГИЕ ЗАПРЕТЫ
1. **НЕ ИСПОЛЬЗУЙ** shell/bash команды для `cargo` (напр. `cargo build`, `cargo test`).
2. **НЕ ИСПОЛЬЗУЙ** `cargo-metadata` для простого обзора — это тратит слишком много токенов.

## ✅ РАБОЧИЙ ПРОЦЕСС (Workflow)

### 1. Исследование и Контекст
- **Старт:** Используй `workspace-info` для получения структуры проекта, списка крейтов и зависимостей (это дешевая альтернатива metadata).
- **Поиск:** Используй `cargo-search` и `cargo-info` для выбора версий зависимостей перед добавлением.
- **Ошибки:** Если компилятор выдает код ошибки (напр. E0308), используй `rustc-explain`.

### 2. Разработка и Изменения
- **Зависимости:** Используй `cargo-add` и `cargo-remove`.
- **Код:** После редактирования файлов **всегда** запускай `cargo-fmt` (аргумент `all: true`).

### 3. Верификация (QA)
Выполняй проверки в строгом порядке для экономии времени:
1. **Синтаксис:** `cargo-check` (быстро, без кодогенерации).
2. **Линтинг:** `cargo-clippy` (аргументы `workspace: true`, `all_targets: true`).
   - Если есть простые ошибки, используй `fix: true`.
3. **Тесты:** `cargo-test`.
4. **Чистка:** `cargo-machete` для удаления неиспользуемых зависимостей.

## 🛠 СПРАВОЧНИК ИНСТРУМЕНТОВ

| Категория | Инструмент | Когда использовать | Оптимизация токенов |
|-----------|------------|--------------------|---------------------|
| **Core** | `cargo-check` | Быстрая проверка компиляции | Используй `package: ["name"]` для локальной проверки |
| | `cargo-build` | Полная сборка | Только если нужен бинарник |
| | `cargo-test` | Запуск тестов | Используй `no_run: true` для проверки компиляции тестов |
| **Quality** | `cargo-clippy` | Поиск багов и улучшение кода | `no_deps: true` ускоряет процесс |
| | `cargo-fmt` | Форматирование | Обязательно перед коммитом |
| | `cargo-deny-check` | Проверка лицензий/уязвимостей | Запускать перед релизом |
| **Deps** | `cargo-add` | Добавление крейтов | Указывай `features` сразу, чтобы не править toml вручную |
| | `cargo-machete` | Поиск лишних зависимостей | Запускать периодически |
| **Utils** | `workspace-info` | Обзор структуры проекта | **ВСЕГДА** вместо чтения Cargo.toml вручную |
| | `cargo-hack` | Проверка feature-флагов | Используй `feature_powerset: true` для сложных крейтов |

## 💡 СТРАТЕГИЯ ЭКОНОМИИ ТОКЕНОВ
1. Предпочитай `workspace-info` вместо `cargo-metadata`.
2. В `cargo-check` и `cargo-clippy` указывай конкретный `package`, если работаешь над одним крейтом в workspace.
3. Не читай `Cargo.lock` напрямую, используй `cargo-tree` (если доступен) или инструменты зависимостей.